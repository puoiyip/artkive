/**make an array of image names for the book
 * on button click, trigger function
 * 
 * idea (written in java)
 * int count = 0 (declare outside of the function)
 * check iteration:
 * if (count == list.length) {
 *      count = 0
 * }
 * for (count; count < list.length; count++){
 *     
 * }
 * 
 * the querySelectorAll() function returns all elements that match the css selector as a nodelist
 * 
 */

// var pageTitle = document.querySelectorAll('h2');
// for (i = 0; i < pageTitle.length; i++) {
//     pageTitle[i].style.
// }